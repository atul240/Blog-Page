<%- include('header') %>

    <h1> Home </h1>
    <p>
        <%= content1 %>
    </p>
    <!-- for(var i=0, i<arr.length; i++){   -->
    <% arr.forEach(function(post){ %>
        <h1>
            <%= post.title%>
        </h1>
        <p>
            <%= post.content.substring(0,100)+"..." %>
                <a href="/posts/<%= post.title %>">
                    Read more
                </a>

        </p>
        <% }) %>
            <!-- console.log(arr[i].title); -->
            <a href="/compose">
                <button
                    style="position: fixed; bottom: 100px; right: 50px; cursor: pointer; font-size: 40px; border-radius: 50%; width: 70px; "
                    class="btn btn-outline-secondary ">+</button></a>

            <button id="printButton"
            style="position: fixed; bottom: 100px; left: 50px; cursor: pointer; border-radius: 50%; width: 70px; height: 70px;"
                class="btn btn-outline-secondary "><span class="material-symbols-outlined">
                    print
                    </span></button>
                

                
             
            <%- include('footer') %>